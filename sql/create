Creation des tables

1 <PRENOM>

Client
Animal

2 <PRENOM>

Veterinaire
Assistant

3 ANNA
Classe espèce
espèce
Medicament

CREATE TABLE classeEspece (

    nom: VARCHAR PRIMARY KEY
);

CREATE TABLE espece (

    nom: VARCHAR PRIMARY KEY,
    classe: VARCHAR NOT NULL,
    FOREIGN KEY(classe) REFERENCES classeEspece(classe)
);

CREATE TABLE medicament (

    nomMolecule: VARCHAR(100) PRIMARY KEY,
    description : VARCHAR NOT NULL
);


4 WILLIAM

Autorisation Medicament
Traitement
Prescription
